import { ArrowRight, Check } from "lucide-react";
import type { PageData } from "@/lib/site-data";
import { Button } from "@/components/ui/button";
import { SiteLink } from "@/components/site-link";
import { Breadcrumbs } from "@/components/service-group-page";
export function ContentPage({data,trail}:{data:PageData;trail?:{label:string;to?:string}[]|undefined}){return <main><section className="page-hero"><img src={data.image} alt={data.imageAlt}/><div className="page-hero-wash"/><div className="zoho-shell page-hero-copy reveal"><p className="zoho-kicker">{data.eyebrow}</p><h1>{data.title}</h1><p>{data.description}</p></div></section>{trail&&<div className="zoho-shell"><Breadcrumbs trail={trail}/></div>}<section className="page-intro reveal"><div className="zoho-shell page-intro-grid"><p className="zoho-kicker">Overview</p><h2>{data.intro}</h2></div></section>{data.note&&<section className="page-note reveal"><div className="zoho-shell">{data.note}</div></section>}{data.sections.map((s,i)=><section className={i%2?"page-section page-section-dark reveal":"page-section reveal"} key={s.title}><div className="zoho-shell page-section-grid"><div><span>0{i+1}</span><h2>{s.title}</h2></div><div><p>{s.body}</p>{s.items&&s.items.length>0&&<ul>{s.items.map(x=><li key={x}><Check/>{x}</li>)}</ul>}</div></div></section>)}<section className="page-cta reveal"><div className="zoho-shell"><div><p className="zoho-kicker">Start a conversation</p><h2>Bring us the technical requirement.</h2></div><Button size="lg" asChild><SiteLink to="/contact">Contact TreeQ Power<ArrowRight/></SiteLink></Button></div></section></main>}
